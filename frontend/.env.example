# Local development only. Never use localhost in Vercel Production settings.
NEXT_PUBLIC_API_URL=http://localhost:8000
# Optional: when absent, derive ws(s)://<API host>/ws/live automatically.
# NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws/live
