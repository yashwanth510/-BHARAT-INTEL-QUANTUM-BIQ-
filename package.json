{
  "name": "biq-monorepo",
  "private": true,
  "workspaces": [
    "frontend",
    "quantum-api"
  ],
  "scripts": {
    "build": "npm run build --workspace=frontend",
    "install": "npm install"
  }
}
